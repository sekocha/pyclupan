# Calculation of Features

## Correlation functions of binary derivative structures
```shell
> pyclupan-feature --cluster pyclupan_cluster.yaml --derivatives pyclupan_derivatives_*
> pyclupan-feature --cluster pyclupan_cluster.yaml --samples pyclupan_sample_attrs.yaml
> pyclupan-feature --cluster pyclupan_cluster.yaml --poscars derivative-1 derivative-1 --element_strings Ag Au
```
